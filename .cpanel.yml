---
deployment:
  tasks:
    - export DEPLOYPATH=/home/rxgqxbxs/etsyagent.afravi.com/
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp -R .htaccess $DEPLOYPATH
    # index.php فقط برای دامنه‌ی بک‌اند Content Agent (agent.beguchee.ir) لازمه —
    # چون به /panel/ ریدایرکت می‌کنه و DirectoryIndex رو روی index.html قاپ می‌زنه
    - /bin/rm -f $DEPLOYPATH/index.php
